package=ai.vespa.secret.config.aws

# Used to create the athenz role name when retrieving secrets on behalf of a tenant
system string
tenant string

# Mapping used to create the AWS role name to assume for retrieving secrets
vaults[].id string
vaults[].name string
vaults[].externalId string
