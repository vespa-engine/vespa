/*
 * This file was generated by the Gradle 'init' task.
 */

dependencies {
    compile project(':container-dependency-versions:parent:component')
    compile project(':container-dependency-versions:parent:messagebus')
    compile project(':container-dependency-versions:parent:vdslib')
    compile project(':container-dependency-versions:parent:vespajlib')
    compile project(':container-dependency-versions:parent:config')
    compile project(':container-dependency-versions:parent:document')
    compile project(':container-dependency-versions:parent:configdefinitions')
    compile 'com.yahoo.vespa:annotations:7.28.21'
    testCompile 'junit:junit:4.12'
    testCompile 'org.mockito:mockito-all:1.9.5'
    compileOnly 'com.google.guava:guava:20.0'
}

task generateConfigurations() {
  configCodegen("${projectDir}/src/main/resources/configdefinitions", "${buildDir}/generated-sources/configgen/")
}

compileJava.dependsOn generateConfigurations

sourceSets {
    main{
        java {
           srcDir "${buildDir}/generated-sources/configgen/"
        }
    }
}
