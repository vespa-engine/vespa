maxtermoccurrences 10000
maxtokenlength 1000
fieldmatchmaxlength 1000000
ilscript[].doctype "specialorder"
ilscript[].docfield[] "a"
ilscript[].docfield[] "b"
ilscript[].docfield[] "c"
ilscript[].docfield[] "d"
ilscript[].docfield[] "e"
ilscript[].docfield[] "f"
ilscript[].docfield[] "g"
ilscript[].docfield[] "h"
ilscript[].content[] "clear_state | guard { input a | tokenize normalize stem:\"BEST\" | index outside_using_a; }"
ilscript[].content[] "clear_state | guard { input a | tokenize normalize stem:\"BEST\" | summary a | index a; }"
ilscript[].content[] "clear_state | guard { \"no\" | set_language; }"
ilscript[].content[] "clear_state | guard { input c | tokenize normalize stem:\"BEST\" | summary c | index c; }"
ilscript[].content[] "clear_state | guard { input d | tokenize normalize stem:\"BEST\" | summary d | index d; }"
ilscript[].content[] "clear_state | guard { input e | set_language | summary e; }"
ilscript[].content[] "clear_state | guard { input f | tokenize normalize stem:\"BEST\" | summary f | index f; }"
ilscript[].content[] "clear_state | guard { \"fr\" | set_language; input g | tokenize normalize stem:\"BEST\" | summary g | index g; }"
ilscript[].content[] "clear_state | guard { input h | tokenize normalize stem:\"BEST\" | summary h | index h; }"
ilscript[].content[] "clear_state | guard { \"en\" | set_language; }"
ilscript[].content[] "clear_state | guard { \"es\" | set_language; }"
ilscript[].content[] "clear_state | guard { input b | tokenize normalize stem:\"BEST\" | index outside_using_b; }"
ilscript[].content[] "clear_state | guard { \"pl\" | set_language; }"
ilscript[].content[] "input b | passthrough b"
